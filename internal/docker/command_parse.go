package docker

import "github.com/hytromo/mimosa/internal/configuration"

func ParseBuildCommand(command []string) (configuration.ParsedCommand, error) {
	return configuration.ParsedCommand{}, nil
}

func ParseBakeCommand(command []string) (configuration.ParsedCommand, error) {
	return configuration.ParsedCommand{}, nil
}
